<UserControl x:Class="ImportApp.WPF.Views.ImportArticleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"   
             xmlns:local="clr-namespace:ImportApp.WPF.Views" xmlns:modalcontrol="clr-namespace:ModalControl;assembly=ModalControl" xmlns:controls="clr-namespace:ImportApp.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#f4f4f4">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <modalcontrol:Modal Panel.ZIndex="1" IsOpen="{Binding IsOpen}" Grid.ColumnSpan="4" Grid.RowSpan="4">
            <controls:MapDataView/>
        </modalcontrol:Modal>


        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1" Grid.ColumnSpan="3" Grid.RowSpan="2" Margin="10">
            <Button Style="{StaticResource importButton}" Grid.Row="1" Grid.Column="0" Command="{Binding OpenDialogCommand}">
                <StackPanel>
                    <iconPacks:PackIconMaterial Style="{StaticResource buttonIcon}" Kind="Upload" Width="100" Height="100"/>
                    <TextBlock Padding="0 5 0 5" TextAlignment="Center"  Text="Upload your ExcelFile"/>
                </StackPanel>
            </Button>

            <Button Style="{StaticResource importButton}" Grid.Row="1" Grid.Column="1" Command="{Binding MapDataCommand}">
                <StackPanel>
                    <iconPacks:PackIconMaterial Style="{StaticResource buttonIcon}" Kind="TableColumn" Width="100" Height="100"/>
                    <TextBlock Padding="0 5 0 5" TextAlignment="Center"  Text="Map columns"/>
                </StackPanel>
            </Button>

            <Button Style="{StaticResource importButton}" Grid.Row="1" Grid.Column="2" Command="{Binding ImportDataCommand}">
                <StackPanel>
                    <iconPacks:PackIconMaterial Style="{StaticResource buttonIcon}" Kind="Import" Width="100" Height="100"/>
                    <TextBlock Padding="0 5 0 5" TextAlignment="Center"  Text="Import to database"/>
                </StackPanel>
            </Button>

        </StackPanel>

    </Grid>
</UserControl>
